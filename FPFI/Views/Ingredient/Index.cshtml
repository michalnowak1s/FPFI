@model IEnumerable<FPFI.Models.Ingredient>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unit.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nutriscore)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Endurance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Allergen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Unit.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nutriscore)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Endurance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Allergen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IngredientID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IngredientID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IngredientID })
        </td>
    </tr>
}

</table>
